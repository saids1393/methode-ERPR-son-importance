# ----------------------------------------------
# 🌍 Project-wide Gradle settings
# ----------------------------------------------

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit:
# https://docs.gradle.org/current/userguide/build_environment.html

# ----------------------------------------------
# 🧠 JVM Configuration
# ----------------------------------------------
# Augmente la mémoire disponible pour Gradle (utile pour les builds Capacitor/Ionic)
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# ✅ Utiliser ton JDK 21 Temurin (vérifie que le chemin existe)
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.8.9-hotspot

# ----------------------------------------------
# ⚙️ Performances et compatibilité
# ----------------------------------------------
# Permet à Gradle d’exécuter des tâches en parallèle
org.gradle.parallel=true

# Active la mise en cache pour accélérer les builds répétés
org.gradle.caching=true

# Force l’utilisation d’AndroidX (obligatoire pour Capacitor / AndroidX libs)
android.useAndroidX=true
android.enableJetifier=true

# ----------------------------------------------
# 🧩 Capacitor / Cordova
# ----------------------------------------------
# Évite les warnings inutiles liés aux chemins longs sous Windows
org.gradle.warning.mode=all
